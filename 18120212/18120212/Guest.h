#ifndef GUEST_H
#define GUEST_H
#include "Header.h"
#include "ListBook.h"
class Guest
{
public:
	Guest();
	virtual ~Guest();
	void pTimSach(ListBook& a);

protected:

private:
};

#endif // GUEST_H
